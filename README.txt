Application requires Scitools Understand to be available via PATH variable before running.

Some tests also require Understand to be available via PATH variable. This variable is updated in the beginning of
gradlew script. In order to perform build properly change that line to the path on your machine.